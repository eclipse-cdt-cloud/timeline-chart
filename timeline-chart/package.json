{
  "name": "timeline-chart",
  "version": "0.0.2",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib"
  },
  "dependencies": {
    "@types/lodash.throttle": "^4.1.4",
    "keyboard-key": "1.1.0",
    "lodash.throttle": "^4.1.1",
    "pixi.js": "^5.0.0",
    "rimraf": "latest"
  }
}
